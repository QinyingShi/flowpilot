# 安全策略

## 支持范围

当前仅维护最新的 `main` 分支。项目仍处于 alpha 阶段，不承诺生产环境的安全 SLA。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交安全报告，不要创建公开 Issue。报告中请包含：

- 受影响的版本或提交
- 可复现步骤和最小验证样例
- 可能的影响范围
- 已知的缓解措施（如有）

维护者会在收到报告后尽快确认，并在修复、验证和发布说明准备完成后协调披露。

## 部署注意事项

- 生产环境必须配置高强度 `PROJECT_API_PROXY_SECRET`，并限制 API 网络边界。
- 使用企业 SSO/OIDC 替代开发身份切换。
- 使用邀请制成员模式并定期审查管理员、项目权限和审计日志。
- 密钥通过部署平台的 Secret 管理功能注入，不写入仓库或前端变量。
- 上传文件应执行类型校验、病毒扫描、大小限制和独立对象存储隔离。
- 启用 AI 或第三方同步前，确认数据分类、跨境、脱敏和留存要求。
